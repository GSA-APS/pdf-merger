# pdf-merger

##
Setup

###
-After cloning repository and have Python setup, run this command in terminal
`pip install -r requirements.txt`

-Replace `Folder_Path` name in `main.py` with location of PDF files to merge and output

-Run in terminal
`python main.py` or `python3 main.py`
